# Release notes

## 1.1.2 — 21 September 2026

- Show the space name in the terminal header instead of the internal default server name.
- Refresh the header when a space is renamed, preserving the active terminal and Settings view.
- Keep named server prefixes to distinguish spaces on different servers.

GitHub release downloads are retained only for the latest version.
